
#include <stdio.h>
#include "gameall.h"
int main(){
   GameAll gameAll;
   gameAll.run();

  // printf("\n winner is %s",gameAll.getWinner());
//    for(int i=0;i<255;i++)
//        printf("   %d   |   %c  \n",i,i);
    return 0;
}
